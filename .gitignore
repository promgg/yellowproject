# ไฟล์ config server (มี path/secret เฉพาะเครื่อง)
server.cfg

# ข้อมูล runtime ที่สคริปต์เขียนทับเอง (ไม่ควร sync ข้ามเครื่อง)
**/used_codes.json
**/user_afk.json
**/*.db
**/*.sqlite

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# preview dev server ของ Claude Code — path เฉพาะเครื่อง คนละพอร์ตกันแต่ละเครื่อง
.claude/launch.json

# Node (ถ้ามีรีซอร์สที่ build ด้วย npm)
node_modules/
dist/
[MJDEV]/MJ-WelfareLogin/data/players.json

# jo_clothingstore/jo_libs ฯลฯ — escrow asset ซื้อมา ผูก key กับ tx เครื่องเซิฟ ไม่ขึ้น git
# (ยกเว้น config/custom/*.lua ที่เราแก้เอง — เก็บสำรองแยกไว้เอง ไม่ sync ผ่าน repo นี้)
# หมายเหตุ: ต้อง escape [ ] เพราะ gitignore ตีความ [Lock] เป็น character class ไม่ใช่ชื่อโฟลเดอร์ตรงตัว
\[Lock\]/
