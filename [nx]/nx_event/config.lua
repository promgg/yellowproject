Config = {}

-- ─── เวลาเริ่มกิจกรรมอัตโนมัติ (HH:MM รูปแบบ 24 ชั่วโมง) ───────────────────
Config.EventTimes = { "08:00", "12:00", "18:00", "22:00" }

-- ─── ระยะเวลากิจกรรม (วินาที) ─────────────────────────────────────────────
Config.EventDuration = 600 -- 10 นาที

-- ─── จำนวนผู้เล่นสูงสุดต่อเมือง ──────────────────────────────────────────
Config.MaxPlayersPerCity = 10

-- ─── จำนวนกล่องทั้งหมดในกิจกรรม ──────────────────────────────────────────
Config.TotalBoxes = 5

-- ─── เวลากดค้างเพื่อเก็บกล่อง (มิลลิวินาที) — phase 1: lp_textui:TextUIHold ──
Config.CollectHoldTime = 900

-- ─── เวลาเล่นอนิเมชั่นเปิดกล่องหลังกดค้างสำเร็จ (มิลลิวินาที) — phase 2: lp_progbar ──
Config.CollectOpenTime = 5000

-- ─── ระยะโต้ตอบกับกล่อง (units) ──────────────────────────────────────────
Config.CollectRadius = 2.0

-- ─── ขอบเขตกิจกรรม ─────────────────────────────────────────────────────────
Config.EventZone = {
    center = vector3(-351.98, 803.01, 115.39), -- Emerald Ranch (ปรับได้)
    radius = 80.0,
}

-- ─── ตำแหน่งสุ่มสำหรับกล่อง (ต้องมีอย่างน้อย TotalBoxes รายการ) ──────────
Config.BoxPositions = {
    vector3(-351.98, 803.01, 115.39),
    vector3(-345.12, 809.54, 115.50),
    vector3(-358.77, 797.33, 115.22),
    vector3(-339.60, 794.88, 115.10),
    vector3(-355.44, 814.72, 115.80),
    vector3(-362.11, 804.22, 115.30),
    vector3(-347.89, 819.44, 115.60),
}

-- ─── Prop ของกล่อง (ชื่อ model ใน RDR2) ──────────────────────────────────
-- "p_gen_crate04x" ไม่มีจริงใน RDR3 (เช็คกับ object list แล้ว) ชื่อจริงคือ p_crate04x
Config.BoxProp = "p_crate04x"

-- ─── อนิเมชั่นขณะเปิดกล่อง ─────────────────────────────────────────────
Config.CollectAnim = {
    dict = "amb_world_human_search_loot_ground@base",
    anim = "base",
}

-- ─── ปุ่มเก็บกล่อง ───────────────────────────────────────────────────────
-- ส่งให้ lp_textui:TextUIHold เป็นคน poll เอง (ปุ่ม E ค่า default ของ lp_textui เอง)
Config.CollectKey = 0x17BEC168 -- ปุ่ม E

-- ─── Blip บน minimap ──────────────────────────────────────────────────────
Config.EventBlip = {
    label = "Event Point", -- RedM เรนเดอร์ไทยบน blip ไม่ได้
    scale = 0.7,
}

-- ─── รางวัลต่อกล่อง (คูณกับ จำนวนกล่องที่เมืองนั้นได้) ─────────────────
-- มอบให้ทุกคนในเมืองนั้นที่เข้าร่วมกิจกรรม
Config.RewardItems = {
    { item = "goldbar",           amount = 1 },
    { item = "herb_burdock_root", amount = 3 },
}

-- ─── ข้อความประกาศเมื่อกิจกรรมเริ่ม ──────────────────────────────────────
Config.EventAnnouncement = "ณ จุดแห่งสัญญา มีสมบัติซ่อนอยู่ ใครมาก่อนได้ก่อนนะ"

-- ─── กลุ่มที่สามารถใช้ /event command ในเกมได้ ──────────────────────────
Config.AdminGroups = { "admin", "superadmin", "moderator" }

-- ─── คำสั่ง /event ─────────────────────────────────────────────────────────
-- /eventstart	เริ่มกิจกรรมทันที
-- /eventstop	หยุดกิจกรรมก่อนเวลา
-- /eventstatus	ดูสถานะ — เวลาเหลือ / ผู้เล่น / กล่องเหลือ
