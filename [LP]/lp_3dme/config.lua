-- ═══════════════════════════════════════════════════════════════════════════
--  lp_3dme — ข้อความ RP ลอยเหนือหัวผู้เล่น
--
--  ⚠️ กติกาความปลอดภัย 3 ข้อที่ห้ามแก้ให้หลวมลง — ระบบแบบนี้พังง่ายมากถ้าทำผิด:
--    1) ข้อความผู้เล่นห้ามต่อเข้า HTML เด็ดขาด ส่งเป็นข้อมูลล้วนแล้วให้ NUI ใช้
--       textContent อย่างเดียว ถ้าเผลอใช้ innerHTML ผู้เล่นพิมพ์ /me <img src=x onerror=...>
--       แล้วรันสคริปต์ใน NUI ของ "ทุกคนที่มองเห็น" ได้ทันที
--    2) ห้ามยิง TriggerClientEvent(..., -1, ...) แล้วให้ client กรองระยะเอง
--       เพราะข้อความจะไปถึงทุกคนทั้งแมพ ดักอ่านได้หมด — server ต้องกรองก่อนส่ง
--    3) ต้องจำกัดความยาวและมี rate limit เสมอ
-- ═══════════════════════════════════════════════════════════════════════════

Config = {}

Config.Debug = false

-- ── ข้อความ ──────────────────────────────────────────────────────────────────
Config.MaxLength    = 150    -- ตัวอักษรสูงสุดต่อข้อความ (server บังคับ ไม่เชื่อ client)
Config.Duration     = 10000  -- ข้อความค้างกี่ ms ก่อนหาย
Config.MaxPerPlayer = 3      -- ข้อความของคนเดียวกันที่ค้างพร้อมกันได้กี่อัน (เกินแล้วดันอันเก่าออก)

-- ความสูงเหนือกระดูกหัว (เมตร) — ปรับถ้ากล่องทับหัวหรือลอยสูงไป
Config.HeightOffset = 0.35

-- ── กันสแปม (server บังคับทั้งหมด) ──────────────────────────────────────────
Config.RateLimit = {
    minIntervalMs   = 1500,  -- เว้นระหว่างข้อความติดกันอย่างน้อยกี่ ms
    maxPerMinute    = 15,    -- เกินนี้ใน 1 นาที = ถูกปฏิเสธจนกว่าจะครบรอบ
}

-- ── ชนิดข้อความ ─────────────────────────────────────────────────────────────
-- key = ชื่อคำสั่ง (พิมพ์ /me, /do, ...)
--   label   ข้อความนำหน้าในกล่อง (ตัวหนาสีตาม color)
--   color   สีของ label
--   range   ระยะที่คนอื่นเห็น (เมตร) — server ใช้ค่านี้กรองผู้รับก่อนส่ง
--   help    คำอธิบายที่โผล่ในช่องแชท (chat:addSuggestion)
--
-- อยากเพิ่มคำสั่งใหม่: เติม entry ที่นี่ที่เดียว ระบบลงทะเบียนคำสั่งจากตารางนี้เอง
Config.Types = {
    ['me'] = {
        label = 'ME', color = '#6ddb51', range = 20.0,
        help = 'บรรยายการกระทำของตัวเอง',
    },
    ['do'] = {
        label = 'DO', color = '#4d66f1', range = 20.0,
        help = 'บรรยายสภาพแวดล้อมหรือสิ่งของ',
    },
    ['say'] = {
        label = 'SAY', color = '#6ddb51', range = 20.0,
        help = 'พูดออกเสียง',
    },
    ['ooc'] = {
        label = 'OOC', color = '#808080', range = 20.0,
        help = 'คุยนอกบท (out of character)',
    },
    ['maskname'] = {
        label = 'ME', color = '#6ddb51', range = 20.0,
        help = 'แสดงชื่อ/ตัวตนที่สวมอยู่',
    },
    ['mefar'] = {
        label = 'ME', color = '#006400', range = 50.0,
        help = 'เหมือน /me แต่คนไกลก็เห็น (50 ม.)',
    },
    ['dofar'] = {
        label = 'DO', color = '#00008B', range = 50.0,
        help = 'เหมือน /do แต่คนไกลก็เห็น (50 ม.)',
    },
    ['dice'] = {
        label = 'DICE', color = '#A52A2A', range = 20.0,
        help = 'ทอยลูกเต๋า — /dice [จำนวนลูก] [จำนวนหน้า]',
    },
    ['roll'] = {
        label = 'ROLL', color = '#FFFF00', range = 20.0,
        help = 'สุ่ม 1-100 ตัดสิน RP',
    },
}

-- ── /desc — ป้ายค้างเหนือหัว (ไม่ใช่ข้อความลอยแล้วหาย) ──────────────────────
-- สั่งพร้อมข้อความ = ตั้งค่า | สั่งเปล่าๆ = ลบทิ้ง
-- เก็บในหน่วยความจำ server เท่านั้น รีสตาร์ทรีซอร์สแล้วหาย
-- ถ้าอยากให้ค้างข้ามวัน ตาราง characters มีช่อง character_desc ว่างอยู่แล้ว
Config.Desc = {
    enabled   = true,
    label     = 'DESC',
    color     = '#c9a227',
    range     = 15.0,
    maxLength = 150,
}

-- ── ลูกเต๋า ──────────────────────────────────────────────────────────────────
-- /dice [ลูก] [หน้า] — ลำดับตาม pgl_play ที่ใช้กันแพร่หลาย (อ่านตรงกับที่คนเขียน 2d6)
-- ผลสุ่มคำนวณฝั่ง server ทั้งหมด client ส่งมาได้แค่ "ขอทอย" ไม่ได้ส่งผลลัพธ์
Config.Dice = {
    defaultCount = 1,
    defaultSides = 6,
    maxCount     = 10,   -- กันทอย 9999 ลูกจนข้อความล้นจอ
    maxSides     = 100,
}

Config.Roll = {
    min = 1,
    max = 100,
}
