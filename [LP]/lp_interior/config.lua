Config = {}

-- ═══════════════════════════════════════════════════════════════════════
--  DEBUG LOG (ส่วนเดิม) — พิมพ์ลง F8 ตอนเข้า/ออก interior
-- ═══════════════════════════════════════════════════════════════════════

-- true = พิมพ์ log ทุก interior ที่เจอ (ใช้ตอนหา id/พิกัดจุดใหม่) — ปิดตอนขึ้น production
Config.DebugLog = true

-- ความถี่ในการเช็คว่าเปลี่ยน interior หรือยัง (ms)
-- 500 = ไวพอสำหรับเดินเข้า/ออกประตู และแทบไม่กิน CPU
Config.PollInterval = 500

-- คำสั่งพิมพ์สถานะปัจจุบันตามต้องการ (ไม่ต้องรอเดินเข้าออก) — false = ไม่ลงทะเบียนคำสั่ง
Config.Command = 'interior'

-- ทศนิยมของพิกัดที่พิมพ์ออกมา
Config.CoordDecimals = 4

-- ═══════════════════════════════════════════════════════════════════════
--  DIMENSION (routing bucket) — ย้ายมิติเมื่อเข้า interior ที่กำหนด
-- ═══════════════════════════════════════════════════════════════════════

Config.Dimension = {
    Enabled = true,

    -- false = ทุกคนที่อยู่ใน interior เดียวกัน "เห็นกัน" (แชร์มิติต่อ interior) ← เหมาะกับ saloon
    -- true  = แต่ละคนได้มิติส่วนตัวของตัวเอง ไม่เห็นใครเลย (เหมาะกับห้องลับ/เนื้อเรื่องเดี่ยว)
    PerPlayer = false,

    -- ให้ NPC/สัตว์ spawn ในมิติใหม่ด้วย ไม่งั้น saloon จะร้างไม่มีคน
    -- (ตั้งค่าตรงๆ เสมอ ไม่พึ่ง default ของ FiveM ที่อาจต่างกันตามเวอร์ชัน)
    EnablePopulation = true,

    -- ระยะผ่อนผันที่ server ใช้ตรวจซ้ำว่าผู้เล่นอยู่ใกล้ interior นั้นจริง (เมตร)
    -- interior บางหลังกว้าง เลยต้องเผื่อเยอะกว่าการตรวจร้านค้าทั่วไป
    ServerDistanceCheck = 60.0,

    -- ── กันอาการ "กระพริบ" ────────────────────────────────────────────
    -- GetInteriorFromEntity คืน 0 เป็นช่วงๆ ทั้งที่ผู้เล่นยังยืนอยู่ข้างใน (พบจริงใน saloon Valentine
    -- ยืนนิ่งๆ แต่ log สลับ เข้า/ออก รัวๆ) ถ้าเชื่อค่าเดียวตรงๆ ผู้เล่นจะโดนเด้งกลับ bucket 0
    -- ทุกไม่กี่วินาที = เห็นคนข้างนอกตลอด
    --
    -- ต้องอ่านได้ 0 ติดกันครบเท่านี้ครั้ง ถึงจะถือว่า "ออกจริง"
    ExitConfirmTicks = 3,
    -- และจะผ่อนผันแบบข้างบนเฉพาะตอนยังอยู่ในระยะนี้จากพิกัดโซน (เมตร)
    -- ถ้าเดินออกไปไกลกว่านี้ = ออกจริงแน่นอน ไม่ต้องรอนับ
    ExitGuardDistance = 30.0,
}

-- ═══════════════════════════════════════════════════════════════════════
--  มิติ AFK — ชั้นที่สองซ้อนบน bucket ปกติของแต่ละ interior
--
--  ผู้เล่นที่กด E ที่ NPC จะถูกย้ายจาก bucket ปกติ (เช่น 7101) ไป afkBucket (7201)
--  ทั้งสองมิติอยู่ในอาคารเดียวกัน แต่มองไม่เห็นกัน — คน AFK จึงไม่รกร้านที่คนอื่น RP อยู่
--  MJ-Afk-Zone-ui เป็นคนสั่งสลับผ่าน export ToggleAfk() แต่ไม่แตะ routing bucket เอง
--  (resource เดียวเท่านั้นที่ควรเรียก SetPlayerRoutingBucket ไม่งั้นสองตัวแย่งกันเขียน)
-- ═══════════════════════════════════════════════════════════════════════
Config.Afk = {
    Enabled = true,
}

-- ═══════════════════════════════════════════════════════════════════════
--  SAFEZONE — เขตปลอดภัย (ตั้งเปิด/ปิดรายจุดที่ฟิลด์ safezone ใน Config.Interiors)
--
--  ⚠️ บังคับได้เฉพาะฝั่ง client เท่านั้น คนใช้โปรแกรมโกงยังยิงได้อยู่ดี
--     ถ้าวันหลังมี interior ที่มีของให้ปล้นจริง ต้องเสริมตัวตรวจดาเมจฝั่ง server เพิ่ม
--
--  ⚠️ ห้ามใช้ RemoveAllPedWeapons เด็ดขาด — vorp_inventory ผูกอาวุธกับ DB คนละชั้น
--     ลบอาวุธออกจาก ped ตรงๆ เสี่ยงของหายจริง ที่นี่ใช้แค่ "บังคับสลับเป็นมือเปล่า"
-- ═══════════════════════════════════════════════════════════════════════
Config.Safezone = {
    Enabled = true,

    -- true = กันดาเมจทุกทาง (SetEntityInvincible) ไม่ใช่แค่ห้ามหยิบปืน
    -- ปิดตัวนี้ = ยังต่อยหมัด/สัตว์กัด/ตกที่สูงตายได้ แต่ยิงกันไม่ได้
    Invincible = true,

    -- เว้นระยะก่อนเด้งข้อความ "หยิบอาวุธไม่ได้" ซ้ำ (ms)
    -- ไม่มีตัวนี้ = ข้อความรัวทุกเฟรมที่ผู้เล่นค้างปุ่มหยิบปืน
    NotifyCooldown = 10000,

    -- อาวุธที่ยกเว้นให้ถือได้ในเขตปลอดภัย (ใส่ชื่อ ไม่ใช่ hash)
    -- ในเกมนี้ตะเกียง/คบไฟนับเป็น "อาวุธ melee" ด้วย ถ้าไม่ยกเว้นจะโดนบังคับเก็บทุกครั้ง
    -- ค่าเริ่มต้นเข้มไว้ก่อน (ไม่ยกเว้นอะไรเลย) — อยากให้ถือตะเกียงในร้านได้ก็ปลดคอมเมนต์
    Allowed = {
        -- 'WEAPON_MELEE_LANTERN',
        -- 'WEAPON_MELEE_DAVY_LANTERN',
    },
}

-- true = แจ้งเตือน pNotify ตอนเข้า/ออกมิติ
Config.Notify = true

Config.Text = {
    Enter = 'เข้าสู่ %s',
    Exit  = 'ออกจาก %s',

    SafezoneEnter  = 'คุณอยู่ในเขตปลอดภัย — ใช้อาวุธไม่ได้',
    SafezoneWeapon = 'เขตปลอดภัย: หยิบอาวุธไม่ได้',

    AfkEnter = 'เข้าสู่มิติพักผ่อน',
    AfkExit  = 'กลับสู่ %s',
}

-- ═══════════════════════════════════════════════════════════════════════
--  รายการ interior ที่จะแยกมิติ
--
--  ⚠️ `coords` = จุดอ้างอิง "ข้างใน" อาคาร ไม่ใช่หน้าประตู
--     ระบบใช้ GetInteriorAtCoords(coords) หา interior id ตอน runtime แทนการ hardcode id
--     เพราะ id เป็นค่าที่อาจเปลี่ยนได้เมื่อเกมอัปเดต ส่วนพิกัดคงที่เสมอ
--     (pattern เดียวกับที่ vorp_zonenotify ใช้เช็คถ้ำ Beaver Hollow)
--     ค่า id ที่จดไว้ในคอมเมนต์มีไว้อ้างอิง/เทียบตอน debug เท่านั้น ไม่ได้ใช้จริง
--
--  ⚠️ bucket ต้องไม่ชนกับ resource อื่น — ในโปรเจกต์นี้ vorp_core/instance.lua ใช้ "source ของ
--     ผู้เล่น" เป็น bucket id (เลขต่ำ 1-30) และ bcc-utils สุ่มเลข เลยเลือกช่วง 7100+ ให้ห่างไว้
--     afkBucket ใช้ช่วง 7200+ ให้แยกกันชัดเจนเวลาอ่าน log
--
--  ── สองโหมดการตรวจจับ ────────────────────────────────────────────────
--  1) โหมด interior (ค่าเริ่มต้น) — ใส่แค่ `coords` ระบบ resolve interior id เอาเอง
--  2) โหมด polygon    — ใส่ `poly` (มุมบนระนาบ x/y) + `minZ`/`maxZ`
--     ใช้กับอาคารที่ "ไม่ใช่ interior ของตัวเกม" เช่นแมพเสริมที่ import เข้ามา
--     GetInteriorAtCoords จะคืน 0 ตลอดกับอาคารพวกนี้ ทำให้โหมดแรกใช้ไม่ได้เลย
--     entry โหมด polygon ยังต้องมี `coords` ไว้ให้ server ใช้ตรวจระยะซ้ำ
-- ═══════════════════════════════════════════════════════════════════════
Config.Interiors = {
    {
        key       = 'saloon_valentine',
        label     = 'Saloon Valentine',
        coords    = vector3(-307.9802, 799.9481, 119.0395), -- id ที่พบตอน debug = 21250
        bucket    = 7101,
        afkBucket = 7201,
        safezone  = true,
    },
    {
        key       = 'saloon_rhodes',
        label     = 'Saloon Rhodes',
        coords    = vector3(1352.4128, -1376.8198, 80.5541), -- id ที่พบตอน debug = 54274
        bucket    = 7102,
        afkBucket = 7202,
        safezone  = true,
    },

    -- Annesburg เป็นแมพเสริม ไม่ใช่ interior ของตัวเกม — GetInteriorAtCoords คืน 0 เสมอ
    -- (log ที่เคยเก็บได้ id 54274 คือบรรทัด "ออก" ของ Rhodes ไม่ใช่ตอนเข้า Annesburg)
    -- จึงใช้โหมด polygon แทน: 4 มุมเดินเก็บจากในเกมจริง
    {
        key       = 'saloon_annesburg',
        label     = 'Saloon Annesburg',
        coords    = vector3(2958.8974, 1349.7712, 45.5), -- จุดกึ่งกลางโดยประมาณ ใช้ตรวจระยะฝั่ง server
        bucket    = 7103,
        afkBucket = 7203,
        safezone  = true,
        poly = {
            vector2(2968.8936, 1355.4669),
            vector2(2955.3442, 1360.4495),
            vector2(2949.3779, 1344.0731),
            vector2(2962.3738, 1339.0953),
        },
        -- ช่วงกว้างโดยตั้งใจ: กันคนอยู่ชั้นบน/บันไดหลุดออกจากโซนเอง
        minZ = 44.0,
        maxZ = 70.0,
    },
}
