Config = {}

-- ═══════════════════════════════════════════════════════════════════════════
--  lp_guard — ตัวกันเซิร์ฟเวอร์ (anti-exploit)
--  ออกแบบให้เพิ่มโมดูลใหม่ทีหลังได้ (เพิ่มตาราง Config.Xxx แล้วเขียน handler ใน server/)
-- ═══════════════════════════════════════════════════════════════════════════

-- ── โมดูล 1: กันชื่อยาว (อุด long-name money-dupe) ──────────────────────────
-- ที่มา: vorp_core SaveCharacterInDb เซฟ money/gold "พร้อมกับ" firstname/lastname/
-- steamname ในคิวรีเดียว ถ้าชื่อ (โดยเฉพาะชื่อ client/Steam จาก GetPlayerName) ยาว
-- เกินขนาดคอลัมน์ DB -> MySQL ปฏิเสธ UPDATE ทั้งก้อน -> เงินสดไม่เคยถูกเซฟลง DB
-- ในขณะที่ยอดแบงก์ (ตาราง bank_users) เซฟผ่านตลอด -> ฝากเงินแล้วออกเกม =
-- เงินสดโรลแบคกลับค่าเดิม + แบงก์เก็บยอดฝากไว้ = ปั๊มเงิน
--
-- โมดูลนี้เด้งผู้เล่นที่ชื่อยาวเกินตั้งแต่ตอน connect (ก่อนเข้าเซิร์ฟ) = ตัดที่ต้นทาง
Config.NameGuard = {
    Enabled = true,

    -- ความยาวชื่อสูงสุด นับเป็น "ตัวอักษร" แบบ UTF-8 (ไม่ใช่ byte)
    -- Steam persona สูงสุด ~32 ตัว ตั้ง 32 จึงไม่เตะผู้เล่นชื่อปกติ
    MaxLength = 32,

    -- ความยาวขั้นต่ำ (0 = ไม่เช็ค) — กันชื่อว่าง/ผิดปกติ
    MinLength = 0,

    -- ข้อความตอนถูกเด้ง (%d ตัวแรก = ความยาวจริง, %d ตัวที่สอง = ลิมิต)
    RejectMessage  = 'ชื่อของคุณยาวเกินไป (%d ตัว) กรุณาเปลี่ยนชื่อใน Settings ให้ไม่เกิน %d ตัว แล้วเข้าใหม่',
    RejectTooShort = 'ชื่อของคุณสั้น/ว่างเกินไป กรุณาตั้งชื่อก่อนเข้าเซิร์ฟเวอร์',

    -- log การเด้งลง server console
    Log = true,
}
